import React from "react"

export default () =>
    <>
        <h1>Primeiro componente</h1>
        <h2>Aprendendo React</h2>
    </>

// As tags vazias enviam o JSX direto para a div root sem nada para os agrupar; O mesmo pode ser feito usando a tag "React.Fragment".

// Este exemplo acima é de um componente baseado em função. E a arrow function usada é bem enxuta, fazendo com que o código fique mais leve e limpo.